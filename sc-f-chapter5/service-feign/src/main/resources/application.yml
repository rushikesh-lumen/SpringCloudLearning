eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

spring.cloud.openfeign.circuitbreaker.enabled: # MIGRATED: feign.hystrix.enabled → true