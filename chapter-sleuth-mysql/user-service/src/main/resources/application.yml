eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: user-service
  zipkin:
    # MIGRATED: spring.zipkin.base-url → management.zipkin.tracing.endpoint
    endpoint: http://localhost:9411/api/v2/spans # MIGRATED: replaced by management.zipkin.tracing.endpoint
  sleuth:
    sampler:
      # MIGRATED: sleuth.sampler.percentage → management.tracing.sampling.probability
      probability: 1.0 # MIGRATED: spring.sleuth.sampler.percentage → management.tracing.sampling.probability

#spring.zipkin.base-url=http://localhost:9411
