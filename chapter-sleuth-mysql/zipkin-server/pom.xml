<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.forezp</groupId>
    <artifactId>zipkin-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>zipkin-server</name>
    <description>Demo project for Spring Boot</description>

    <parent>
        <groupId>com.forezp</groupId>
        <artifactId>sleuth</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>


    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId><!-- MIGRATED: spring-cloud-starter-eureka renamed to spring-cloud-starter-netflix-eureka-client -->
        </dependency>
<!-- MIGRATED: io.zipkin.java embedded server is NOT compatible with Spring Boot 3.x.
     In Spring Boot 3.x, run Zipkin as a standalone server:
       docker run -d -p 9411:9411 openzipkin/zipkin
     Or download from: https://zipkin.io/pages/quickstart.html -->
		<!--
        <dependency>
            <groupId>io.zipkin.java</groupId>
            <artifactId>zipkin-server</artifactId>
            <version>1.19.0</version>
        </dependency>
		-->

<!-- MIGRATED: io.zipkin.java embedded server is NOT compatible with Spring Boot 3.x.
     In Spring Boot 3.x, run Zipkin as a standalone server:
       docker run -d -p 9411:9411 openzipkin/zipkin
     Or download from: https://zipkin.io/pages/quickstart.html -->
		<!--
        <dependency>
            <groupId>io.zipkin.java</groupId>
            <artifactId>zipkin-storage-mysql</artifactId>
            <version>1.19.0</version>
        </dependency>
		-->

<!-- MIGRATED: io.zipkin.java embedded server is NOT compatible with Spring Boot 3.x.
     In Spring Boot 3.x, run Zipkin as a standalone server:
       docker run -d -p 9411:9411 openzipkin/zipkin
     Or download from: https://zipkin.io/pages/quickstart.html -->
		<!--
        <dependency>
            <groupId>io.zipkin.java</groupId>
            <artifactId>zipkin-autoconfigure-ui</artifactId>
        </dependency>
		-->

        <dependency>
            <groupId>com.mysql</groupId><!-- MIGRATED: mysql:mysql-connector-java → com.mysql:mysql-connector-j (renamed in MySQL 8.x) -->
            <artifactId>mysql-connector-j</artifactId><!-- MIGRATED: mysql-connector-java renamed to mysql-connector-j -->
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>


</project>
