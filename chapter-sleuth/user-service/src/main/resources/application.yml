eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: user-service
  zipkin:
    base-url: http://localhost:9411
  sleuth: # MIGRATED: updated sleuth config
    sampler:
      probability: # MIGRATED: renamed from percentage to probability (Spring Cloud Sleuth 3.x) 1.0

#spring.zipkin.base-url=http://localhost:9411
